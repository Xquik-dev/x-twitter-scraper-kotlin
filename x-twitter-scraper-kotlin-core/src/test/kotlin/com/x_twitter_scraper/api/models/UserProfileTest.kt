// SPDX-FileCopyrightText: 2026 Xquik contributors
//
// SPDX-License-Identifier: Apache-2.0

// File generated from our OpenAPI spec by Stainless.

package com.x_twitter_scraper.api.models

import com.fasterxml.jackson.module.kotlin.jacksonTypeRef
import com.x_twitter_scraper.api.core.JsonValue
import com.x_twitter_scraper.api.core.jsonMapper
import org.assertj.core.api.Assertions.assertThat
import org.junit.jupiter.api.Test

internal class UserProfileTest {

    @Test
    fun create() {
        val userProfile =
            UserProfile.builder()
                .id("9876543210")
                .name("Elon Musk")
                .username("elonmusk")
                .affiliatesHighlightedLabel(
                    UserProfile.AffiliatesHighlightedLabel.builder()
                        .badgeUrl("badgeUrl")
                        .description("description")
                        .url("url")
                        .urlType("urlType")
                        .userLabelDisplayType("userLabelDisplayType")
                        .userLabelType("userLabelType")
                        .build()
                )
                .automatedBy("example_user")
                .businessAccountAffiliatesCount(0L)
                .communityRole("Member")
                .coverPicture("https://pbs.twimg.com/profile_banners/example.jpg")
                .createdAt("2009-06-02T20:12:29Z")
                .creatorSubscriptionsCount(0L)
                .description("CEO of Tesla, SpaceX, and X")
                .favouritesCount(18000L)
                .followers(150000000L)
                .following(500L)
                .hasCustomTimelines(true)
                .hasGraduatedAccess(true)
                .hasHiddenSubscriptionsOnProfile(true)
                .highlightsInfo(
                    UserProfile.HighlightsInfo.builder()
                        .canHighlightTweets(true)
                        .highlightedTweets("highlightedTweets")
                        .build()
                )
                .identityVerification(
                    UserProfile.IdentityVerification.builder()
                        .description("description")
                        .isIdentityVerified(true)
                        .verifiedSinceMsec("verifiedSinceMsec")
                        .build()
                )
                .isAutomated(false)
                .isBlueVerified(true)
                .isProfileTranslatable(true)
                .isTranslator(false)
                .isVerified(true)
                .location("Austin, TX")
                .mediaCount(1200L)
                .parodyCommentaryFanLabel("parodyCommentaryFanLabel")
                .addPinnedTweetId("1234567890")
                .possiblySensitive(false)
                .profileBio(
                    UserProfile.ProfileBio.builder()
                        .putAdditionalProperty("description", JsonValue.from("bar"))
                        .putAdditionalProperty("entities", JsonValue.from("bar"))
                        .build()
                )
                .profileBannerUrl("https://pbs.twimg.com/profile_banners/example.jpg")
                .profileDescriptionLanguage("profileDescriptionLanguage")
                .profileImageShape("profileImageShape")
                .profileInterstitialType("profileInterstitialType")
                .profilePicture("https://pbs.twimg.com/profile_images/example.jpg")
                .profileSortEnabled(true)
                .profileTranslatorType("profileTranslatorType")
                .protected(false)
                .statusesCount(35000L)
                .superFollowEligible(true)
                .unavailable(false)
                .unavailableReason("suspended")
                .url("https://xquik.com")
                .verified(true)
                .verifiedType("Business")
                .addWithheldInCountry("DE")
                .build()

        assertThat(userProfile.id()).isEqualTo("9876543210")
        assertThat(userProfile.name()).isEqualTo("Elon Musk")
        assertThat(userProfile.username()).isEqualTo("elonmusk")
        assertThat(userProfile.affiliatesHighlightedLabel())
            .isEqualTo(
                UserProfile.AffiliatesHighlightedLabel.builder()
                    .badgeUrl("badgeUrl")
                    .description("description")
                    .url("url")
                    .urlType("urlType")
                    .userLabelDisplayType("userLabelDisplayType")
                    .userLabelType("userLabelType")
                    .build()
            )
        assertThat(userProfile.automatedBy()).isEqualTo("example_user")
        assertThat(userProfile.businessAccountAffiliatesCount()).isEqualTo(0L)
        assertThat(userProfile.communityRole()).isEqualTo("Member")
        assertThat(userProfile.coverPicture())
            .isEqualTo("https://pbs.twimg.com/profile_banners/example.jpg")
        assertThat(userProfile.createdAt()).isEqualTo("2009-06-02T20:12:29Z")
        assertThat(userProfile.creatorSubscriptionsCount()).isEqualTo(0L)
        assertThat(userProfile.description()).isEqualTo("CEO of Tesla, SpaceX, and X")
        assertThat(userProfile.favouritesCount()).isEqualTo(18000L)
        assertThat(userProfile.followers()).isEqualTo(150000000L)
        assertThat(userProfile.following()).isEqualTo(500L)
        assertThat(userProfile.hasCustomTimelines()).isEqualTo(true)
        assertThat(userProfile.hasGraduatedAccess()).isEqualTo(true)
        assertThat(userProfile.hasHiddenSubscriptionsOnProfile()).isEqualTo(true)
        assertThat(userProfile.highlightsInfo())
            .isEqualTo(
                UserProfile.HighlightsInfo.builder()
                    .canHighlightTweets(true)
                    .highlightedTweets("highlightedTweets")
                    .build()
            )
        assertThat(userProfile.identityVerification())
            .isEqualTo(
                UserProfile.IdentityVerification.builder()
                    .description("description")
                    .isIdentityVerified(true)
                    .verifiedSinceMsec("verifiedSinceMsec")
                    .build()
            )
        assertThat(userProfile.isAutomated()).isEqualTo(false)
        assertThat(userProfile.isBlueVerified()).isEqualTo(true)
        assertThat(userProfile.isProfileTranslatable()).isEqualTo(true)
        assertThat(userProfile.isTranslator()).isEqualTo(false)
        assertThat(userProfile.isVerified()).isEqualTo(true)
        assertThat(userProfile.location()).isEqualTo("Austin, TX")
        assertThat(userProfile.mediaCount()).isEqualTo(1200L)
        assertThat(userProfile.parodyCommentaryFanLabel()).isEqualTo("parodyCommentaryFanLabel")
        assertThat(userProfile.pinnedTweetIds()).containsExactly("1234567890")
        assertThat(userProfile.possiblySensitive()).isEqualTo(false)
        assertThat(userProfile.profileBio())
            .isEqualTo(
                UserProfile.ProfileBio.builder()
                    .putAdditionalProperty("description", JsonValue.from("bar"))
                    .putAdditionalProperty("entities", JsonValue.from("bar"))
                    .build()
            )
        assertThat(userProfile.profileBannerUrl())
            .isEqualTo("https://pbs.twimg.com/profile_banners/example.jpg")
        assertThat(userProfile.profileDescriptionLanguage()).isEqualTo("profileDescriptionLanguage")
        assertThat(userProfile.profileImageShape()).isEqualTo("profileImageShape")
        assertThat(userProfile.profileInterstitialType()).isEqualTo("profileInterstitialType")
        assertThat(userProfile.profilePicture())
            .isEqualTo("https://pbs.twimg.com/profile_images/example.jpg")
        assertThat(userProfile.profileSortEnabled()).isEqualTo(true)
        assertThat(userProfile.profileTranslatorType()).isEqualTo("profileTranslatorType")
        assertThat(userProfile.protected()).isEqualTo(false)
        assertThat(userProfile.statusesCount()).isEqualTo(35000L)
        assertThat(userProfile.superFollowEligible()).isEqualTo(true)
        assertThat(userProfile.unavailable()).isEqualTo(false)
        assertThat(userProfile.unavailableReason()).isEqualTo("suspended")
        assertThat(userProfile.url()).isEqualTo("https://xquik.com")
        assertThat(userProfile.verified()).isEqualTo(true)
        assertThat(userProfile.verifiedType()).isEqualTo("Business")
        assertThat(userProfile.withheldInCountries()).containsExactly("DE")
    }

    @Test
    fun roundtrip() {
        val jsonMapper = jsonMapper()
        val userProfile =
            UserProfile.builder()
                .id("9876543210")
                .name("Elon Musk")
                .username("elonmusk")
                .affiliatesHighlightedLabel(
                    UserProfile.AffiliatesHighlightedLabel.builder()
                        .badgeUrl("badgeUrl")
                        .description("description")
                        .url("url")
                        .urlType("urlType")
                        .userLabelDisplayType("userLabelDisplayType")
                        .userLabelType("userLabelType")
                        .build()
                )
                .automatedBy("example_user")
                .businessAccountAffiliatesCount(0L)
                .communityRole("Member")
                .coverPicture("https://pbs.twimg.com/profile_banners/example.jpg")
                .createdAt("2009-06-02T20:12:29Z")
                .creatorSubscriptionsCount(0L)
                .description("CEO of Tesla, SpaceX, and X")
                .favouritesCount(18000L)
                .followers(150000000L)
                .following(500L)
                .hasCustomTimelines(true)
                .hasGraduatedAccess(true)
                .hasHiddenSubscriptionsOnProfile(true)
                .highlightsInfo(
                    UserProfile.HighlightsInfo.builder()
                        .canHighlightTweets(true)
                        .highlightedTweets("highlightedTweets")
                        .build()
                )
                .identityVerification(
                    UserProfile.IdentityVerification.builder()
                        .description("description")
                        .isIdentityVerified(true)
                        .verifiedSinceMsec("verifiedSinceMsec")
                        .build()
                )
                .isAutomated(false)
                .isBlueVerified(true)
                .isProfileTranslatable(true)
                .isTranslator(false)
                .isVerified(true)
                .location("Austin, TX")
                .mediaCount(1200L)
                .parodyCommentaryFanLabel("parodyCommentaryFanLabel")
                .addPinnedTweetId("1234567890")
                .possiblySensitive(false)
                .profileBio(
                    UserProfile.ProfileBio.builder()
                        .putAdditionalProperty("description", JsonValue.from("bar"))
                        .putAdditionalProperty("entities", JsonValue.from("bar"))
                        .build()
                )
                .profileBannerUrl("https://pbs.twimg.com/profile_banners/example.jpg")
                .profileDescriptionLanguage("profileDescriptionLanguage")
                .profileImageShape("profileImageShape")
                .profileInterstitialType("profileInterstitialType")
                .profilePicture("https://pbs.twimg.com/profile_images/example.jpg")
                .profileSortEnabled(true)
                .profileTranslatorType("profileTranslatorType")
                .protected(false)
                .statusesCount(35000L)
                .superFollowEligible(true)
                .unavailable(false)
                .unavailableReason("suspended")
                .url("https://xquik.com")
                .verified(true)
                .verifiedType("Business")
                .addWithheldInCountry("DE")
                .build()

        val roundtrippedUserProfile =
            jsonMapper.readValue(
                jsonMapper.writeValueAsString(userProfile),
                jacksonTypeRef<UserProfile>(),
            )

        assertThat(roundtrippedUserProfile).isEqualTo(userProfile)
    }
}
